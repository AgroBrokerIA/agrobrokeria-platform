"use client";
import {useEffect,useState} from "react";
import {supabase} from "@/lib/supabase/client";
export default function Terminos(){const[d,setD]=useState<any[]>([]),[err,setErr]=useState("");useEffect(()=>{supabase.from("documentos_legales").select("id,codigo,titulo,version,vigencia_desde,contenido,hash_sha256,estado").order("codigo").order("version",{ascending:false}).then(({data,error})=>{if(error)setErr(error.message);else setD(data||[])})},[]);return <main className="module-page"><div className="module-hero"><div><span className="eyebrow">LEGAL</span><h1>Términos y políticas</h1><p>Versiones publicadas y vigentes de la documentación de AgroBrokerIA.</p></div></div>{err&&<div className="module-alert module-alert-error">{err}</div>}<section className="company-grid">{d.map(x=><article className="company-card" key={x.id}><div className="company-card-head"><div><h2>{x.titulo}</h2><p>Versión {x.version} · {x.estado}</p></div><span className="company-status">{new Date(x.vigencia_desde).toLocaleDateString("es-AR")}</span></div><p>{x.contenido}</p><small>Hash SHA-256: {x.hash_sha256}</small></article>)}</section></main>}