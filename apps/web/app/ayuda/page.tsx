"use client";
import {useEffect,useMemo,useState} from "react";
import {supabase} from "@/lib/supabase/client";
export default function AyudaPage(){const [a,setA]=useState<any[]>([]),[q,setQ]=useState(""),[err,setErr]=useState("");useEffect(()=>{supabase.from("ayuda_articulos").select("id,categoria,titulo,contenido").eq("activo",true).order("categoria").order("titulo").then(({data,error})=>{if(error)setErr(error.message);else setA(data||[])})},[]);const f=useMemo(()=>a.filter(x=>(x.titulo+" "+x.contenido+" "+x.categoria).toLowerCase().includes(q.toLowerCase())),[a,q]);return <main className="module-page"><div className="module-hero"><div><span className="eyebrow">SOPORTE</span><h1>Centro de ayuda</h1><p>Guías y respuestas sobre el uso comercial de AgroBrokerIA.</p></div><div className="module-pill">Ayuda</div></div><input aria-label="Buscar ayuda" value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar por ejemplo: SISA, contratos, mensajes..." style={{width:"100%",padding:14,border:"1px solid #dbe3ea",borderRadius:12,marginBottom:20}}/>{err&&<div className="module-alert module-alert-error">{err}</div>}<section className="company-grid">{f.map(x=><article className="company-card" key={x.id}><span className="eyebrow">{x.categoria}</span><h2>{x.titulo}</h2><p>{x.contenido}</p></article>)}</section>{!f.length&&!err&&<div className="module-empty"><h2>No encontramos resultados</h2><p>Probá con otra palabra.</p></div>}</main>}